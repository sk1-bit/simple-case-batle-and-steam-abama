[package]
name = "hvh-cases"
version = "1.0.0"
description = "HVH.CASES — Case Simulator"
authors = ["HVH"]
edition = "2021"
rust-version = "1.77"

[lib]
name = "hvh_cases_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
